import { Meta, Markdown } from '@storybook/addon-docs/blocks';
import { convert, ThemeProvider, themes } from 'storybook/theming';

import JS_README from '../../src/utils/README.md?raw';

<Meta title='Utilities/@js' />

# Utils

```ts
import /* ... from */ '@psychobolt/circle-ui-elements/utils/*';
```

<ThemeProvider theme={convert(themes.normal)}>
  <Markdown>{JS_README.replace('# Utils', '')}</Markdown>
</ThemeProvider>
